public class Streams_Used {
    // In Java, there are several types of streams that are commonly used for input and output operations. These streams can be categorized into two main types: byte streams and character streams.

    // 1. Byte Streams: Byte streams are used to read and write binary data. They operate on raw bytes and are typically used for handling binary files such as images, audio files, and other non-text data. The main classes for byte streams are:
    // - InputStream: This is the abstract class for reading byte streams. Common subclasses include FileInputStream, BufferedInputStream, and DataInputStream.
    // - OutputStream: This is the abstract class for writing byte streams. Common subclasses include FileOutputStream, BufferedOutputStream, and DataOutputStream.

    // 2. Character Streams: Character streams are used to read and write text data. They operate on characters and are typically used for handling text files. The main classes for character streams are:
    // - Reader: This is the abstract class for reading character streams. Common subclasses include FileReader, BufferedReader, and InputStreamReader.
    // - Writer: This is the abstract class for writing character streams. Common subclasses include FileWriter, BufferedWriter, and OutputStreamWriter.

    // Additionally, there are also specialized streams such as ObjectInputStream and ObjectOutputStream for serializing and deserializing objects.
    
}
